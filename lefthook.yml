pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: '**/*.{js,jsx,ts,tsx}'
      run: bunx oxlint --deny-warnings {staged_files}
    - name: fmt-check
      glob: '**/*.{js,jsx,json,md,ts,tsx,yaml,yml}'
      run: bunx oxfmt --check --config .oxfmt.json {staged_files}
